import 'package:flutter/material.dart';

class RestOrders extends StatelessWidget {
  const RestOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Orders"),
    );
  }
}